/**
 * Pro dialog (disabled - app uses local storage only)
 */
export function ProModelDialog({
  isOpen,
  setIsOpen,
  currentModel
}: {
  isOpen: boolean;
  setIsOpen: (isOpen: boolean) => void;
  currentModel: string
}) {
  return null
}
